# Core dependencies for Performance RNN
#
# RECOMMENDED INSTALLATION:
# Please use the conda environment files instead:
#   - environment-cpu.yml for CPU-only systems
#   - environment-cuda.yml for NVIDIA GPU systems
#   - environment-mps.yml for Apple Silicon Macs
#
# Install with: conda env create -f environment-<your-system>.yml
#
# This requirements.txt file is provided for pip-only installations.
# Note: You must install PyTorch separately before using this file.
#
# For pip installation:
#   1. Install PyTorch first: https://pytorch.org/get-started/locally/
#   2. Then run: pip install -r requirements.txt

numpy>=1.21.0,<2.0.0
pretty_midi>=0.2.9
tqdm>=4.60.0
tensorboard>=2.10.0
optuna>=3.0.0
pandas>=1.3.0
