# Conda environment for Performance RNN (Apple Silicon with MPS)
# Install with: conda env create -f environment-mps.yml
# Activate with: conda activate py_magenta

name: py_magenta

channels:
  - pytorch
  - conda-forge
  - defaults

dependencies:
  - python=3.10

  # PyTorch (with MPS support for Apple Silicon)
  - pytorch>=2.0.0
  - torchvision
  - torchaudio

  # Core dependencies
  - numpy>=1.21.0,<2.0.0
  - pandas>=1.3.0
  - tqdm>=4.60.0

  # TensorBoard for logging
  - tensorboard>=2.10.0

  # pip dependencies (not available via conda or better installed via pip)
  - pip
  - pip:
    - pretty_midi>=0.2.9
    - optuna>=3.0.0
