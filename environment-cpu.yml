# Conda environment for Performance RNN (CPU-only)
# Install with: conda env create -f environment-cpu.yml
# Activate with: conda activate py_magenta

name: py_magenta

channels:
  - pytorch
  - conda-forge
  - defaults

dependencies:
  - python=3.10

  # PyTorch (CPU-only)
  - pytorch>=2.0.0
  - torchvision
  - torchaudio
  - cpuonly

  # Core dependencies
  - numpy>=1.21.0,<2.0.0
  - pandas>=1.3.0
  - tqdm>=4.60.0

  # TensorBoard for logging
  - tensorboard>=2.10.0

  # pip dependencies (not available via conda or better installed via pip)
  - pip
  - pip:
    - pretty_midi>=0.2.9
    - optuna>=3.0.0
